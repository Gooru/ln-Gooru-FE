<div class="inspect-destination-container">
  <div class="destination-chart-container">
    {{proficiency.student-inspect-competency-chart competencyStatus=competencyStatus isProficiency=isProficiency title=gradeData.grade route0Contents=route0Contents isRoute0Chart=isRoute0 classId=classId courseId=courseId classGrade=classGrade subjectCode=subjectCode onSelectGrade="onSelectGrade"}}
  </div>
  {{#if (eq type "route")}}
    <div class="course-container">
      {{#if (and isRoute0Pending isRoute0)}}
        <div class="pre-study-title">
          {{t "student-first-experience.preStudyTitle" title=gradeData.grade}}
        </div>
        <div class="route0-container">
          <div class="route0-comment" {{action "onToggleRoute0View"}}>
            <div class="suggestion-icon">{{gru-icon name="explore_outline"}}</div>
            <div class="suggestion-note"> {{t 'class.overview.course-map.route0-bannerdesc'}}</div>
          </div>
          <div class="route0-body" style="display: none;">
            <div class="route0-accordion-container">
              {{class.overview.course-suggestions model=route0Contents index=index class=classData course=courseData}}
            </div>
          </div>
        </div>
        <div class="route0-action">
          <div class="ignore" {{action "onRoute0Accept"}}>
            <i></i>
            <span>{{t 'student-first-experience.route0-action.ignore'}}</span>
          </div>
          <div class="accept" {{action "onRoute0Reject"}}>
            <i></i>
            <span>{{t 'student-first-experience.route0-action.accept'}}</span>
          </div>
        </div>
      {{/if}}
      <div class="assigned-course-title">
        {{t "student-first-experience.assigned-course-title" title=gradeData.grade}}
      </div>
      <div class="course-map-accordion-container">
        {{student.class.destination.student-destination-course-accordion
        courseData=courseData
        classId=classId
        courseId=courseId
        isRoute0=isRoute0
      }}
      </div>
      <div class="study-course" {{action onMoveNext 'playNext'}}>
        {{gru-icon name="directions"}} {{t "student-first-experience.study-course"}}
      </div>
    </div>
    {{else if (eq type "destination")}}
    <div class="grade-data-container">
      <div class="grade-title">
        {{gradeData.grade}}
      </div>
      <div class="grade-description">
        {{safe-html gradeData.description}}
      </div>
      <div class="next-route" {{action onMoveNext 'inspect-destination'}}>
        {{gru-icon name="directions"}} {{t "student-first-experience.show-me-my-route"}}
      </div>
    </div>
  {{else}}
    <div class="competency-level-container">
      <div class="grade-title">
        {{'t' "student-first-experience.competency-level.title"}}
      </div>
      <div class="grade-description">
        {{'t' "student-first-experience.competency-level-description.description_1" grade=gradeData.grade}} {{'t' "student-first-experience.competency-level-description.description_2"}} {{'t' "student-first-experience.competency-level-description.description_3"}}
      </div>
      <div class="competency-level-list">
        <div class="mastery-container">
          <div class="legend"></div>
          <div class="label"> {{t "student-first-experience.competency-level.mastery" count=competencyStatus.master}}</div>
        </div>
        <div class="in-progress-container">
          <div class="legend"></div>
          <div class="label"> {{t "student-first-experience.competency-level.in-progress" count=competencyStatus.inProgress}}</div>
        </div>
        <div class="not-started-container">
          <div class="legend"></div>
          <div class="label"> {{t "student-first-experience.competency-level.not-started" count=competencyStatus.notStarted}}</div>
        </div>
        <div class="skyline-container">
          <div class="legend"></div>
          <div class="label"> {{t "student-first-experience.competency-level.your-skyline"}}</div>
        </div>
      </div>
      <div class="show-destination" {{action onMoveNext 'proficiency'}}>
        {{gru-icon name="directions"}} {{t "student-first-experience.view-my-destination"}}
      </div>
    </div>
  {{/if}}
</div>
