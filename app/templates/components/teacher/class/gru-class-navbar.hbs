<div class="header-panel" {{action 'closeNotificationList'}}>
  <div class="nav-body">
    <div class="app-close">
      {{#link-to 'index' class="navbar-brand home-link material-icons close" }}
        <i class="gru-icon material-icons close">close</i>
        {{!-- {{gru-icon name='close'}} --}}
      {{/link-to}}
    </div>
    {{#unless class.isArchived}}
      <div class="atc tab">
        <a {{action 'selectItem' 'atc'}} class="atc-link" title="{{t 'teacher-landing.class.performance-overview'}}" role="tab" data-toggle="tooltip">
          <span class="atc-menu"></span>
          <span class="add-info">{{t 'teacher-landing.class.performance-overview'}}</span>
        </a>
      </div>
      <div class="class-activities tab">
        <a {{action 'selectItem' 'class-activities'}} class="dca" title="{{t 'teacher-landing.class.class-activities'}}" role="tab" data-toggle="tooltip">
          <span class="dca-pin"></span>
          <span class="add-info">{{t 'teacher-landing.class.class-activities'}}</span>
        </a>
      </div>
      <div class="course-map tab">
        <a {{action 'selectItem' 'course-map'}} role="tab" data-toggle="tooltip" title="{{t 'teacher-landing.class.course-map'}}">
          {{gru-icon name='sort'}}
          <span class="add-info">{{t 'teacher-landing.class.course-map'}}</span>
        </a>
      </div>
      <div class="students tab">
        <a {{action 'selectItem' 'students'}} role="tab" data-toggle="tooltip" title="{{t 'teacher-landing.class.student-proficiency' }}">
          {{gru-icon name='account_circle'}}
          <span class="add-info">{{t 'teacher-landing.class.student-proficiency' }}</span>
        </a>
      </div>
    {{/unless}}
    {{#if navTitle}}
      <div class="studyplayer-title">
        <span>{{navTitle}}</span>
      </div>
    {{/if}}
    {{#if isFeatureEnabled }}
      <div class="notification-tab">
        {{notification/notification-indicator displayNotificationList=displayNotificationList accessorClass='class' isTeacher=true classId=class.id }}
      </div>
    {{/if}}
    <div class="performance-summary">
      {{#if isOfflineClass}}
        <div class="dca-performance {{unless performanceSummaryForDCA.performance.scoreInPercentage 'disabled'}}" {{action 'onOpenOCAReport'}} style="background-color:{{if performanceSummaryForDCA (grade-color performanceSummaryForDCA.performance.scoreInPercentage)}}">
          {{#if performanceSummaryForDCA.performance.scoreInPercentage}}
            {{performanceSummaryForDCA.performance.scoreInPercentage}}%
          {{else}}
            --
          {{/if}}
        </div>
      {{else}}
        <div class="{{unless hasStarted 'not-started'}}" {{action 'onOpenCourseReport'}}>
          {{charts/gru-performance-chart performanceSummary=class.performanceSummary }}
        </div>
      {{/if}}
    </div>
    <div class="class-management tab ">
      <a {{action 'selectItem' 'class-management'}} role="tab " data-toggle="tooltip " title=" {{t 'teacher-landing.class.class-management'}} ">
        {{gru-icon name="settings"}}
      </a>
    </div>
  </div>
</div>