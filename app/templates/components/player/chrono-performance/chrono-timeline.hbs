<div class="container">
  <div class="header">
    {{player.chrono-view.chrono-header performanceSummary=performanceSummary activities=activities onSelectCard="onSelectCard"}}
  </div>
  <div class="body class-activities" id="carousel">
    {{#each activities as |activitiy index|}}
      {{player.chrono-view.student-card  activitiy=activitiy cardIndex=index onSelectCard="onSelectCard"}}
    {{/each}}
  </div>
</div>
