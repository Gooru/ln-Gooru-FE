{{!-- <div class="associated-rubric">
    <div class="image">
        <i class="{{submission.taskSubmissionSubType}}-icon"></i>
    </div>
    <div class="info" style="width:90%">
        <h3>{{submission.id}} </h3>
    </div>
    {{!-- {{task.description}} --}}
{{!-- <div class="actions">
        <button class="btn no-bgd remove-item" {{action 'removeLineItem'}}>
            {{gru-icon name="remove_circle"}}
        </button>
    </div>
</div> --}}
{{log 'submission' submission}}
{{!-- <div class='upload-list-container'>
    <span class="uploaded-item"> --}}
<div class="btn no-bgd add-item" {{action 'updateSubmissionCollection' submission.taskSubmissionSubType }}>{{gru-icon name="add_circle"}}</div>
<span class="image">
    <i class="{{submission.taskSubmissionSubType}}-icon"></i>
</span>
<span class="countItem">
    {{submission.count}}
</span>
{{#if (gt submission.count 0)}}
    <button class="remove-uploaded btn no-bgd remove-item" {{action 'removeLineItem' submission.refData }}>{{gru-icon name="remove_circle"}}</button>
{{/if}}
{{!-- </span>
</div> --}}