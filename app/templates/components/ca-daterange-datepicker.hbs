<div class="backdrop"></div>
<div class="datepickers {{if isOfflineActivity 'offline'}}">
  <div class="start-date">
    <span class="{{unless isOfflineActivity 'hidden'}}">{{t 'goals.manage.start-date-label'}}</span>
    {{ca-date-picker
       userStartDateAsToday=true
       onSelectDate='onScheduleStartDate'
       showMonths=(if isOfflineActivity false true)
       onSelectMonth='onScheduleCAForMonth'
       forFirstDateOfMonth=forFirstDateOfMonth
     }}
  </div>
  {{#if isOfflineActivity}}
    <div class="end-date">
      <span>{{t 'goals.manage.end-date-label'}}</span>
      {{ca-date-picker
       userStartDateAsToday=true
       onSelectDate='onScheduleEndDate'
       startDate=startDate
       showMonths=false
       validateEndDate=true
     }}
    </div>
  {{/if}}
</div>
{{#if isOfflineActivity}}
  <div class="months">
    {{ca-month-picker onSelectMonth='onScheduleOAForMonth' currentMonth=forFirstDateOfMonth}}
  </div>
{{/if}}
