{{#if showPullUp}}
  <div class="overlay" {{action 'onClosePullUp' true}}></div>
{{/if}}
<div class="competency-report">
  <div class="header">
    <div class="competency-status status-{{competency.status}}"></div>
    <div class="report-info">
      <div class="title-section {{if competency 'content'}}">
        <div class="domain-name">{{competency.domainName}}</div>
        <div class="title-content">
        <div class="competency-code">{{competency.competencyCode}}</div>
      </div>
        {{#unless competency}}
          <div class="title">
            {{title}}
          </div>
        {{/unless}}
      </div>
      {{#if competency}}
        <div class="competency-status">
          {{competencyStatus}}
          {{#if isBadgedCompetency}}
            <div class="badge">
            </div>
          {{/if}}
        </div>
      {{/if}}
    </div>
    <div class="close-pullup" {{action "onClosePullUp"}}>
      {{gru-icon name="close"}}
    </div>
  </div>
  <div class="body">
    {{yield}}
  </div>
</div>
