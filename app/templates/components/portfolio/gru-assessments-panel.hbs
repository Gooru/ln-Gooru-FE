<div class="assessments-container">
  <div class="header-container">
    <div class="icon"></div>
    <div class="title">Assessments</div>
    <div class="search-items-container">
      <div class="filter-search" {{action "onOpenFilter"}}>
        {{gru-icon name="filter_list"}}
      </div>
    </div>
  </div>
  <div class="body-container {{if isShowContentFilters 'filter-populated'}}">
    <div class="assessments">
      {{#each learnedPortfolioItems as | assessment |}}
          {{portfolio/gru-portfolio-content-card content = assessment}}
      {{/each}}
    </div>
  </div>
  {{#if isShowContentFilters}}
    {{portfolio/gru-content-filters
      refreshFilters = "refreshFilters"
      pickerStartDate = userCreatedAt
      userPreference = userPreference}}
  {{/if}}
</div>
